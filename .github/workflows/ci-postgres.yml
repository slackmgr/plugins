name: CI (postgres)

on:
  push:
    branches: [ main ]
    paths:
      - 'postgres/**'
      - '.golangci.yaml'
      - '.github/workflows/ci-postgres.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'postgres/**'
      - '.golangci.yaml'
      - '.github/workflows/ci-postgres.yml'
  workflow_dispatch:

permissions:
  security-events: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Setup Go
      uses: actions/setup-go@v6.2.0
      with:
        go-version-file: 'postgres/go.mod'
        cache: true
        cache-dependency-path: 'postgres/go.sum'

    - name: Run go fmt
      working-directory: postgres
      run: |
        unformatted=$(gofmt -l .)
        if [ -n "$unformatted" ]; then
          echo "The following files are not properly formatted:"
          echo "$unformatted"
          exit 1
        fi

    - name: Run go vet
      working-directory: postgres
      run: go vet ./...

    - name: Run tests
      working-directory: postgres
      run: |
        go test -race -cover ./... 2>&1 | tee /tmp/test-output.txt
        test_exit=${PIPESTATUS[0]}
        echo "## Test Coverage" >> $GITHUB_STEP_SUMMARY
        grep "coverage:" /tmp/test-output.txt >> $GITHUB_STEP_SUMMARY || true
        exit $test_exit

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Set up Go
      uses: actions/setup-go@v6.2.0
      with:
        go-version-file: 'postgres/go.mod'
        cache: true
        cache-dependency-path: 'postgres/go.sum'

    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v9.2.0
      with:
        version: latest
        args: --timeout=1m
        working-directory: postgres

  security:
    name: Security
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Run Gosec Security Scanner
      uses: securego/gosec@v2.23.0
      with:
        args: '-fmt sarif -out results.sarif ./postgres/...'

    - name: Upload Gosec results
      if: always()
      uses: github/codeql-action/upload-sarif@v4.32.3
      with:
        sarif_file: results.sarif
