name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  security-events: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test (${{ matrix.plugin }})
    runs-on: ubuntu-latest

    strategy:
      matrix:
        plugin: [sqs, dynamodb, postgres]

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Setup Go
      uses: actions/setup-go@v6.2.0
      with:
        go-version-file: '${{ matrix.plugin }}/go.mod'
        cache: true

    - name: Run go fmt
      working-directory: ${{ matrix.plugin }}
      run: |
        unformatted=$(gofmt -l .)
        if [ -n "$unformatted" ]; then
          echo "The following files are not properly formatted:"
          echo "$unformatted"
          exit 1
        fi

    - name: Run go vet
      working-directory: ${{ matrix.plugin }}
      run: go vet ./...

    - name: Run tests
      working-directory: ${{ matrix.plugin }}
      run: |
        go test -race -cover ./... 2>&1 | tee /tmp/test-output.txt
        test_exit=${PIPESTATUS[0]}
        echo "## Test Coverage (${{ matrix.plugin }})" >> $GITHUB_STEP_SUMMARY
        grep "coverage:" /tmp/test-output.txt >> $GITHUB_STEP_SUMMARY || true
        exit $test_exit

  lint:
    name: Lint (${{ matrix.plugin }})
    runs-on: ubuntu-latest

    strategy:
      matrix:
        plugin: [sqs, dynamodb, postgres]

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Set up Go
      uses: actions/setup-go@v6.2.0
      with:
        go-version-file: '${{ matrix.plugin }}/go.mod'
        cache: true

    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v9.2.0
      with:
        version: latest
        args: --timeout=1m
        working-directory: ${{ matrix.plugin }}

  security:
    name: Security (${{ matrix.plugin }})
    runs-on: ubuntu-latest

    strategy:
      matrix:
        plugin: [sqs, dynamodb, postgres]

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Run Gosec Security Scanner
      uses: securego/gosec@v2.23.0
      with:
        args: '-no-fail -fmt sarif -out results.sarif ./${{ matrix.plugin }}/...'

    - name: Upload Gosec results
      uses: github/codeql-action/upload-sarif@v4.32.3
      with:
        sarif_file: results.sarif
