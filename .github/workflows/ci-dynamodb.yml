name: CI (dynamodb)

on:
  push:
    branches: [ main ]
    paths:
      - 'dynamodb/*.go'
      - 'dynamodb/go.mod'
      - 'dynamodb/go.sum'
      - '.golangci.yaml'
      - '.github/workflows/ci-dynamodb.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'dynamodb/*.go'
      - 'dynamodb/go.mod'
      - 'dynamodb/go.sum'
      - '.golangci.yaml'
      - '.github/workflows/ci-dynamodb.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Setup Go
      uses: actions/setup-go@v6.2.0
      with:
        go-version-file: 'dynamodb/go.mod'
        cache: true
        cache-dependency-path: 'dynamodb/go.sum'

    - name: Run go fmt
      working-directory: dynamodb
      run: |
        unformatted=$(gofmt -l .)
        if [ -n "$unformatted" ]; then
          echo "The following files are not properly formatted:"
          echo "$unformatted"
          exit 1
        fi

    - name: Run go vet
      working-directory: dynamodb
      run: go vet ./...

    - name: Run tests
      working-directory: dynamodb
      run: |
        go test -race -cover ./... 2>&1 | tee /tmp/test-output.txt
        test_exit=${PIPESTATUS[0]}
        echo "## Test Coverage" >> $GITHUB_STEP_SUMMARY
        grep "coverage:" /tmp/test-output.txt >> $GITHUB_STEP_SUMMARY || true
        exit $test_exit

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Set up Go
      uses: actions/setup-go@v6.2.0
      with:
        go-version-file: 'dynamodb/go.mod'
        cache: true
        cache-dependency-path: 'dynamodb/go.sum'

    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v9.2.0
      with:
        version: latest
        args: --timeout=1m
        working-directory: dynamodb

  security:
    name: Security
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6.0.2

    - name: Set up Go
      uses: actions/setup-go@v6.2.0
      with:
        go-version: 'stable'
        cache: true
        cache-dependency-path: 'dynamodb/go.sum'

    - name: Install gosec
      run: go install github.com/securego/gosec/v2/cmd/gosec@latest

    - name: Install govulncheck
      run: go install golang.org/x/vuln/cmd/govulncheck@latest

    - name: Run Gosec Security Scanner
      working-directory: dynamodb
      run: gosec ./...

    - name: Run govulncheck
      working-directory: dynamodb
      run: govulncheck ./...
